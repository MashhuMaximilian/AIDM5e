--- slash_commands.py
+++ slash_commands.py
@@ -108,6 +108,8 @@ def _color_from_name(color_name: str) -> discord.Color:
         "random": discord.Color.random(),
     }
 
+    if color_name is None:
+        color_name = "DarkBlue"
     normalized = color_name.strip().lower().replace(" ", "")
 
     # Check if it's a hex color
